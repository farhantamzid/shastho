[build]
  command = "pip install --upgrade pip && apt-get update -y && apt-get install -y libpq-dev && pip install -r requirements.txt && python app.py"
  publish = "."

[build.environment]
  PYTHON_VERSION = "3.10"